version: '3.8'

services:
  signage-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: home-signage
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./logs:/app/logs
      - ./file-repo:/app/file-repo
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET}
      - SQLITE_PATH=/app/data/signage.db
      - LOG_LEVEL=info
      - FILE_REPO_PATH=/app/file-repo
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
